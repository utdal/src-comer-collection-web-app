import React from "react";
import EntityDeleteButton from "../Entity/EntityDeleteButton";

const ImageDeleteButton = (): React.JSX.Element => {
    return (
        <EntityDeleteButton />
    );
};

export default ImageDeleteButton;
