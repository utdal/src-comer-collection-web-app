import React from "react";
import { EntityManageRestoreButton } from "../Entity/EntityManageRestoreButton.js";

const ImageRestoreButton = () => {
    return (
        <EntityManageRestoreButton
            disabled={false}
        />
    );
};

export default ImageRestoreButton;
