import React from "react";
import RestoreButton from "../Entity/RestoreButton";

const ImageRestoreButton = (): React.JSX.Element => {
    return (
        <RestoreButton />
    );
};

export default ImageRestoreButton;
