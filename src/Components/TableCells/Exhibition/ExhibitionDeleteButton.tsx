import React from "react";
import EntityDeleteButton from "../Entity/EntityDeleteButton";

const ExhibitionDeleteButton = (): React.JSX.Element => {
    return (
        <EntityDeleteButton disabled={false} />
    );
};

export default ExhibitionDeleteButton;
