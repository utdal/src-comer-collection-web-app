import React from "react";
import EntityEditButton from "../Entity/EntityEditButton";

const UserEditButton = (): React.JSX.Element => {
    return (
        <EntityEditButton />
    );
};

export default UserEditButton;
